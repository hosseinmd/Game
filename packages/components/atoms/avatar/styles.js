import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    height: 60,
    margin: 5,
    borderRadius: 30,
    overflow: 'hidden',
  },
  image: {
    width: 60,
    height: 60,
  },
});
